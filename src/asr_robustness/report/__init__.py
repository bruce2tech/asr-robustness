"""Analysis and reporting: turn decode results into curves, tables, and figures.

`analyze` aggregates raw per-utterance results into per-condition metrics;
`plots` and `tables` render them; `build` is the CLI that produces the full
report directory.
"""
